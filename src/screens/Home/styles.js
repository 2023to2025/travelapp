import { StyleSheet } from "react-native";

const style = StyleSheet.create ({
    container: {
        margin: 32,
    },
    subTitle: {
        color: 'black',
        fontSize: 20,
        marginTop: 40,
        marginBottom: 30,
    }
});

export default style;