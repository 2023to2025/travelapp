import { StyleSheet } from "react-native";

const style = StyleSheet.create ({
    container: {
        margin: 32,
    },
    subTitle: {
        color: 'black',
        fontSize: 20,
        marginTop: 40,
        marginBottom: 10,
    }
});

export default style;